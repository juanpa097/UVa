#include "bits/stdc++.h"
using namespace std;
int main(int argc, char const *argv[]) {
  string s;
  int c;
  while (cin >> s) {
    if (s == "end") break;
    map<string, stack<int> > cont;

    for (int i = 0; i < s.size(); ++i) {

    }

  }
  return 0;
}
